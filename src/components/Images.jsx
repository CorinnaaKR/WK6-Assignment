export default function Images(props) {
    const imageData = {}
    return (
     <section className="thumbnails">      
        <div>{title={props.title} img src={props.url} alt={props.alt}        
        {/* < onClick={handleClick} /> */}}
        </div>
</section>
    );
}